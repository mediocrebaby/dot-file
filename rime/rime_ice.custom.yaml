patch:
  grammar:
    language: amz-v2n3m1-zh-hans
    collocation_max_length: 5
    collocation_min_length: 2
  translator/contextual_suggestions: true
  translator/max_homophones: 7
  translator/max_homographs: 7
  # ascii_mode 状态导出:在 processor 列表最前端插入,init() 尽早连接 notifier
  # func() 永远返回 kNoop(透传),对现有输入逻辑零干扰;⚠️ 切勿改为 kRejected(会拦截所有按键)
  'engine/processors/@before 0': lua_processor@ascii_mode_export