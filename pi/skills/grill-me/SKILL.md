---
name: grill-me
description: 写代码/改文件前必须先跑一遍的歧义排查流程：逐类检查目标、范围、实现方式、格式位置、完成标准五类未定义项，只要有一项靠猜就立刻用 ask_user_question 一轮一题问清楚再开工（每题 2–4 个选项），避免臆想开工导致返工。TRIGGER——新增功能/修 bug/重构/搭原型/写脚本等请求缺少范围、目标、依赖或格式；同一需求存在多种明显不同的实现路径；出现「优化一下/完善/改进/这个/相关的地方」等模糊表述；关键信息（技术栈、样式、文件位置、完成标准）无法从代码或上下文明确读出；以及任何你正准备写「我先假设/默认按 X 处理」的时刻。不要因为「改动很小/大概能猜到/先写了再说」而跳过。SKIP——请求本身已完全明确；纯信息查询或代码解释；用户已在上下文里给出足够约束；仅涉及可合理默认的风格微调。
---

# grill-me

动手前先把关键歧义问清楚。宁可多问一轮，不要基于臆想开工返工。

## 强制流程（不可跳过）

加载本技能后，**在任何 Edit / Write / 大段实现之前**，必须依次完成：

1. **查证**：用 `read` / `ls` / `rg` 把能从代码和上下文查到的答案查清楚。
2. **逐类过清单**：对照下面「五类常见歧义」，逐类列出本次请求里仍未确定的项，形成歧义清单。禁止跳过任何一类而直接宣称「没有歧义」。
3. **清单非空 → 立刻调用 `ask_user_question`**；清单为空才可以直接开工。

不允许先动手再问，也不允许「边做边确认」。发现歧义的那一刻就停下提问。

## 判定标准：任一条成立就必须问

对清单里的每一项自问：

- 按我的猜测做，猜错了用户是否要返工？ → 是，就问。
- 这一项存在两条以上都说得通的做法，且结果明显不同？ → 是，就问。
- 我正准备在回复里写「我假设 / 默认采用 / 这里先按 X 处理」？ → 别假设，直接问。

只有当答案能从代码、目录结构、用户已有表述里**明确读出来**时才算「可推断」，才可以不问。「看起来大概是这个意思」「通常人们都这么做」不算可推断。

用户已在上下文里表过态的偏好，不要再问一遍。

## 容易被漏掉的信号词

出现下列表述时几乎必然存在未定义项，默认要问：

- 模糊动词：优化一下、改进、完善、增强、修一下、整理、支持一下 X
- 指代不清：这个、那部分、相关的地方、类似的都改了
- 缺验收标准：只说做什么，没说做到什么程度算完
- 范围开口：涉及多个文件/模块，但没说改哪些、要不要连带改
- 新增产物：要新建文件/命令/接口，但没说放哪、叫什么、什么形态

## 五类常见歧义

- **目标**：想解决什么问题？给谁用？为什么现在做？
- **范围**：改哪些文件、是否涉及相邻代码、是否处理旧数据或兼容性?
- **实现方式**：多种合理路径并存时（引入依赖 vs 自己写、库 A vs 库 B、CLI vs GUI、同步 vs 异步）
- **格式与位置**：文件放哪、命名规则、输出结构、是否需要测试或文档
- **完成标准**：什么算做完？需要覆盖哪些用例？是否要跑通某些命令？

## 提问规范（使用 ask_user_question）

**一轮只问一个问题**：每次调用 `ask_user_question` 的 `questions` 数组只放一道题。问完等用户回答，再根据已确定的信息决定下一轮问什么，直到所有模糊点都澄清完毕才开工。一轮一题的好处：后面的问题可以基于前面的答案重新措辞或直接省掉，选项也不会一屏堆满。

顺序上，先问最能影响后续走向的那个问题 —— 它的答案会让后面的问题变多、变少或改写。不要因为想少轮就把互相依赖的选择塞进同一题。

对每题：

- `header` ≤ 16 字符的短标签，如 "Auth 方式"、"目标目录"、"数据源"
- 问题体是完整问句，以问号结尾
- 提供 2–4 个选项，每个选项：
  - `label` 1–5 词，≤ 60 字符
  - `description` 一句话解释选项含义或权衡
  - **不要**手动写 "其他" / "Other" / "Type something." ——系统会自动追加自由输入行，手动写会被运行时拒绝
- 有推荐选项时，把它放第一个，`label` 末尾追加 ` (Recommended)`
- 多个答案同时合理时，设 `multiSelect: true`
- 需要视觉对比（代码片段、布局、配置）时，单选题可加 `preview` markdown 字段做侧栏预览

## 反例

❌ 明明能从代码里读出来还问 —— 先用 `read` / `ls` / `rg` 查一遍
❌ 一轮塞多题、堆一屏选项让用户挑 —— 每轮只问一题，等回答再接下一题
❌ 当前问题的选项里塞进本应该属于后续问题的子选择 —— 保持单题职责单一，子选择留到下一轮
❌ 把纯偏好（缩进 2 还是 4、单双引号）当成关键决策来问
❌ 用提问替代思考 —— 通用技术惯例（错误向上传播、不硬编码密钥、复用已有工具等）按默认做法处理，不必问；但凡涉及用户主观选择或多种合理实现路径，必须问，不要私自替用户拍板
❌ 问完就等下一条消息才动手 —— 拿到答案立刻执行
❌ 怕打断用户、怕显得啰嗦而硬着头皮猜 —— 一轮提问的成本远低于一次返工
❌ 在正文里用自然语言「顺便问一下……」 —— 提问必须走 `ask_user_question` 工具，纯文字提问不算数
❌ 先写完再在结尾附一句「不对我可以改」 —— 这是把决策成本推给用户，等同于跳过流程

## 拿到答案后

- 先判断是否还有未澄清的模糊点：有 → 紧接着发起下一轮问下一道题；无 → 进入开工
- 不要在两题之间插入大段确认性对话，一句话复述已拿到的选择即可，直接接下一题
- 全部澄清后立即开工，不要把所有选择重新招呼一遍再确认
- 若用户走了自由输入（"Type something."），把它当成明确指令处理
- 若用户按 Esc 放弃问答，后续不要再追问，回退到最保守的默认方案并在动手前明确声明所用假设
- 开工过程中新冒出未定义项（读代码后才发现的分叉），同样停下按上述流程提问，不要顺手替用户拍板
