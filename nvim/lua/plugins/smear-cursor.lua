return {
  "sphamba/smear-cursor.nvim",
  event = "VeryLazy",
  opts = {
    -- 拖尾"头部"向目标移动的速度。0=不动，1=瞬间到位；
    -- 值越小头部跟随越慢、动画越柔。官方默认 0.6
    stiffness = 0.5,
    -- 拖尾"尾部"向目标移动的速度。0=不动，1=瞬间到位；
    -- 值越小尾巴拖得越长越飘。官方默认 0.45
    trailing_stiffness = 0.2,
    -- 速度的阻尼（随时间衰减）。0=不衰减，1=完全衰减；
    -- 值越大越快停下、回弹越少。官方默认 0.85
    damping = 0.6,
    -- 拖尾纵向亮度渐变的指数。0=无渐变（整条等亮），1=线性渐变。官方默认 1.0
    gradient_exponent = 1,
  },
}
